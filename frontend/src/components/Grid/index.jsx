import PropTypes from 'prop-types';

function Grid ({
    children,
}) {
    

    return (  
        <div>

        </div>
    );
}

Grid.propTypes = {
    children: PropTypes.node
}

export default Grid;